import {StyleSheet} from 'react-native';

export const mainScreenStyles = StyleSheet.create({
  screen: {
    flex: 1,
    backgroundColor: '#FFF',
    alignItems: 'center',
  },
  annotationList: {
    marginVertical: 10,
    flexGrow: 0,
  },
});
